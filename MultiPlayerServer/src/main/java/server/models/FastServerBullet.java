package server.models;


import server.models.server.Bullet;

public class FastServerBullet extends Bullet {
    public FastServerBullet(server.models.Bullet bullet, float r, float g, float b) {
        super(bullet, r, g, b);
    }
}

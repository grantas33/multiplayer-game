package server.side.models;


import server.side.models.server.Bullet;

public class FastServerBullet extends Bullet {
    public FastServerBullet(server.side.models.Bullet bullet, float r, float g, float b) {
        super(bullet, r, g, b);
    }
}
